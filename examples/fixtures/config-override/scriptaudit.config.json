{
  "knownSafe": ["publish:dry"],
  "blocked": ["verify"],
  "rules": [
    {
      "match": "clean",
      "risk": "caution",
      "reason": "Allowed cleanup command, but it still deletes generated files."
    }
  ]
}
