# Agent CLI Fixture

This fixture gives ScriptAudit a small command surface to classify.

```bash
npm run build
npm run check
npm run smoke
npm run clean
npm run publish:dry
npm run deploy
```

Use it for documentation demos only. The fixture is intentionally small and is not a package to publish.
